/* eslint max-len: 0 */
export const environmentConstants = {
  // Development constants
  development: {
    example: false
  },
  // Production constants
  production: {
    example: true
  },
  // YorkLab (production) constants
  yorklab: {
    example: true
  }
};

// Common constants
// e.g. not affected by the environment
export const commonConstants = {
  example: false
};
